Install options:

1. Local development:
   cargo install --path /Users/turnercore/Projects/CLI/elephant-never-forgets-cli --locked

2. Release script after a tagged GitHub release exists:
   curl -fsSL https://raw.githubusercontent.com/Elephant-Hand-Games/elephant-never-forgets-cli/main/scripts/install.sh | sh

